/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SalutePipe } from './salute.pipe';

describe('SalutePipe', () => {
  it('create an instance', () => {
    const pipe = new SalutePipe();
    expect(pipe).toBeTruthy();
  });
});
